# 招投标文档智能分析系统 - Nginx 配置
# 此配置已合并到 /etc/nginx/conf.d/kb-claw.conf 中

# 实际配置位置：/etc/nginx/conf.d/kb-claw.conf
# 配置内容：
# location /tendering/ {
#     alias /usr/share/nginx/html/tendering/;
#     index index.html;
#     try_files $uri $uri/ /tendering/index.html;
# }

# 访问地址：http://127.0.0.1/tendering/

# 部署步骤（已完成）：
# 1. 构建前端项目：cd /root/.openclaw/workspace/tendering/src && npm run build
# 2. 复制构建文件：cp -r dist/* /usr/share/nginx/html/tendering/
# 3. 配置已合并到 kb-claw.conf
# 4. 重载 Nginx：nginx -s reload
